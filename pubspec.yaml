name: smart_warmth_2025
description: Applicazione per il controllo del riscaldamento ecologico domestico.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter


  # UI
  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.9
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.4.4
  fl_chart: ^0.69.0

  # Stato
  flutter_riverpod: ^2.4.9

  # Navigazione
  go_router: ^13.1.0

  # Storage
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  graphql_flutter: ^5.1.2

  # Rete
  dio: ^5.4.0
  connectivity_plus: ^5.0.2

  # Utilità
  intl: ^0.19.0
  logger: ^2.0.2+1
  email_validator: ^3.0.0
  url_launcher: ^6.1.10
  
  # BlueTooth\Wifi
  flutter_reactive_ble: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/bootsplash_logo.png
    - assets/bootsplash_logo_1200.png
    - assets/bootsplash_logo_450.png
    - assets/bootsplash_logo_600.png
    - assets/bootsplash_logo_900.png
    - assets/bootsplash_manifest.json
    - assets/images/
    - assets/images/icona-app-smart-warmth_old.png
    - assets/images/logo_app.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
